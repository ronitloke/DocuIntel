# Demo screenshots

This directory is reserved for reviewed screenshots of the local Streamlit
application. No screenshots are included yet, so the public README uses a
textual demo checklist instead of broken image links.

Suggested future captures:

- `home.png`
- `documents.png`
- `grounded-qa.png`
- `analyze.png`
- `compare.png`
- `privacy.png`
- `evaluation.png`

Before adding a screenshot, remove personal filenames, local paths, document
contents, API tokens, and other machine-specific details.
